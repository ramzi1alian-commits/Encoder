<?xml version="1.0" encoding="utf-8"?>
<!--
    Android 12+ (API 31+) backup/transfer rules. android:allowBackup="false"
    in the manifest already disables Auto Backup entirely, but this file is
    added as an explicit, defense-in-depth second layer covering the two
    Android-12+ data paths specifically: cloud backup and device-to-device
    transfer (e.g. the "copy my data" flow when setting up a new phone) -
    both are disabled outright so accent/theme prefs (or anything added to
    this file's scope in the future) can never leave the original device.
-->
<data-extraction-rules>
    <cloud-backup disableIfNoEncryptionCapability="true">
        <exclude domain="sharedpref" path="." />
        <exclude domain="database" path="." />
        <exclude domain="file" path="." />
    </cloud-backup>
    <device-transfer>
        <exclude domain="sharedpref" path="." />
        <exclude domain="database" path="." />
        <exclude domain="file" path="." />
    </device-transfer>
</data-extraction-rules>
